В Linux є кілька зручних способів подивитися, **які процеси навантажують процесор** 👇

### 🔹 1. `top` — стандартна утиліта

```bash
top
```

* `%CPU` — показує завантаження процесора процесом
* Сортування за CPU: натисни **`P`**
* Вийти: **`q`**

---

### 🔹 2. `htop` — зручніша альтернатива (якщо встановлена)

```bash
htop
```

Якщо немає:

```bash
sudo apt install htop
```

Переваги:

* Кольоровий інтерфейс
* Мишка
* Легко видно навантаження по ядрах CPU

---

### 🔹 3. `ps` — швидкий знімок

Показати топ процесів за CPU:

```bash
ps aux --sort=-%cpu | head
```

Або тільки PID, назву і CPU:

```bash
ps -eo pid,comm,%cpu --sort=-%cpu | head
```

---

### 🔹 4. `mpstat` — завантаження по ядрах

```bash
mpstat -P ALL 1
```

(потрібен пакет `sysstat`)

---

### 🔹 5. Подивитися конкретний процес

```bash
top -p <PID>
```

---

### 🔹 6. В реальному часі (простий варіант)

```bash
watch -n 1 "ps -eo pid,comm,%cpu --sort=-%cpu | head"
```

---

### ✅ Що краще використовувати?

* **Швидко** → `top`
* **Зручно і наочно** → `htop`
* **Для скриптів** → `ps`

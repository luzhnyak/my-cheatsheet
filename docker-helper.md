### Щоб побачити всі **запущені контейнери** в Docker, використай команду:

```bash
docker ps
```

Вона покаже список активних контейнерів із інформацією: `CONTAINER ID`, `IMAGE`, `COMMAND`, `STATUS`, `PORTS`, `NAMES`.

Якщо хочеш побачити **всі контейнери** (і запущені, і зупинені), додай прапорець `-a`:

```bash
docker ps -a
```

👉 Якщо потрібно лише ID контейнерів:

```bash
docker ps -q
```

### Є кілька способів зупинити запущені контейнери в Docker:

1. **Зупинити один контейнер за його ID або ім’ям**:

```bash
docker stop <container_id_or_name>
```

2. **Зупинити всі запущені контейнери одразу**:

```bash
docker stop $(docker ps -q)
```

3. **Якщо хочеш зупинити і одразу видалити** (обережно 🚨):

```bash
docker rm -f $(docker ps -q)
```

> `-f` (force) означає: спершу зупинити, потім видалити.

### Зробити повну очистку у два кроки:

1. **Зупинити всі контейнери**

```bash
docker stop $(docker ps -q)
```

2. **Видалити всі зупинені контейнери + непотрібні образи + volume + network**

```bash
docker system prune -a --volumes
```

* `-a` — видаляє всі непотрібні образи (не лише "dangling").
* `--volumes` — також видаляє невикористані томи.
* Перед виконанням Docker запитає підтвердження.

👉 Якщо хочеш зробити це **без підтвердження**, додай `-f`:

```bash
docker system prune -a --volumes -f
```




